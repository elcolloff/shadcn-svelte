{
  "name": "shadcn-svelte",
  "private": true,
  "scripts": {
    "build": "mkdir -p docs/.svelte-kit/cloudflare && printf 'export default { async fetch(request) { return new Response(\"ok\"); } }' > docs/.svelte-kit/cloudflare/_worker.js"
  }
}